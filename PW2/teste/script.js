const botaoExibir = document.getElementById("exibir");
const BotaoPaRelogo = document.getElementById("PaRelogo");

botaoExibir.addEventListener("click", function() {
    const dia = parseInt(document.getElementById(diaAtual).value);
    const mes = parseInt(document.getElementsById(mesAtual).value);
    const ano = parseInt(document.getElementById("anoAtual").value);
    
    const noemMes = document.getElementById("nomeMes");
    const horaAtual = parseInt(document.getElementById("horaAtual").value);
    const minutoAtual = parseInt(document.getElementById("minutoAtual").value);
    const segundoAtual = parseInt(document.getElementById("segundoAtual".value));

    const diaSemana = document.getElementById("diaSemana");

})
